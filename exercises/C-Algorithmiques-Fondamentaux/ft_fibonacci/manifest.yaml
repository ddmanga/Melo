name: ft_fibonacci
tags: ["piscine", "ex4", "arithmetique"]

sources:
  - ft_fibonacci.c

compile:
  compiler: cc
  flags:
    - -Wall
    - -Wextra
    - -Werror

tests:

  - name: "indice 0 vaut 0"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(0);
          if (res != 0)
          {
              printf("ft_fibonacci(0) = %d, attendu 0\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "indice 1 vaut 1"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(1);
          if (res != 1)
          {
              printf("ft_fibonacci(1) = %d, attendu 1\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "indice 2 vaut 1 (0 + 1)"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(2);
          if (res != 1)
          {
              printf("ft_fibonacci(2) = %d, attendu 1\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "indice 3 vaut 2"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(3);
          if (res != 2)
          {
              printf("ft_fibonacci(3) = %d, attendu 2\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "indice 4 vaut 3"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(4);
          if (res != 3)
          {
              printf("ft_fibonacci(4) = %d, attendu 3\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "indice 10 vaut 55"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(10);
          if (res != 55)
          {
              printf("ft_fibonacci(10) = %d, attendu 55\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "indice 20 vaut 6765"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(20);
          if (res != 6765)
          {
              printf("ft_fibonacci(20) = %d, attendu 6765\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "indice negatif : -1 retourne -1"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(-1);
          if (res != -1)
          {
              printf("ft_fibonacci(-1) = %d, attendu -1\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "indice negatif quelconque retourne -1"
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(-42);
          if (res != -1)
          {
              printf("ft_fibonacci(-42) = %d, attendu -1\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0

  - name: "pas de stack overflow sur un indice tres negatif"
    # Meme piege que sur ft_recursive_power / ft_recursive_factorial : si le
    # test "index < 0" n'est verifie qu'au tout premier appel, une fonction
    # recursive mal ecrite peut ne jamais atteindre son cas de base et faire
    # un stack overflow au lieu de retourner -1 immediatement.
    main: |
      #include <stdio.h>
      #include <limits.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(INT_MIN + 1);
          if (res != -1)
          {
              printf("ft_fibonacci(INT_MIN + 1) = %d, attendu -1\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0
    check_memory: false

  - name: "temps d'execution raisonnable pour un indice modere (pas de memoisation requise mais pas de blocage)"
    # ft_fibonacci(30) implique environ 2.7 millions d'appels recursifs avec
    # l'implementation naive (sans memoisation), ce qui reste executable en
    # quelques centaines de ms sur une machine normale. Ce test verifie juste
    # que la fonction ne part pas dans une explosion combinatoire ingerable
    # ou une recursion infinie deguisee.
    main: |
      #include <stdio.h>
      int ft_fibonacci(int index);
      int main(void)
      {
          int res = ft_fibonacci(30);
          if (res != 832040)
          {
              printf("ft_fibonacci(30) = %d, attendu 832040\n", res);
              return (1);
          }
          return (0);
      }
    expected_exit: 0
    timeout: 10