# ft_is_negative

Répertoire : `ex0/`
Fichiers à rendre : `ft_is_negative.c`
Autorisé : `write`

Créez une fonction qui affiche 'N' ou 'P' selon le signe de l'entier passé
en paramètre. Si n est négatif, affichez 'N'. Si n est positif ou nul,
affichez 'P'.

Prototype : `void ft_is_negative(int n);`